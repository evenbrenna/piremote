<!DOCTYPE html>
<html>

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <title>PiRemote</title>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
        <script src="js/jquery.ui.touch-punch.min.js"></script>
        <script src="js/dynamics.min.js"></script>
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="style/style.css">
    </head>

    <body>
        <div class="container">
            <div class="row vertical-center-row">
                <div class="col-xs-12 col-sm-6 text-left">

                    <h1>Videos <span class="btn btn-default" id="showVideos">Hide</span></h1>
                    <p>Click a video to play. Click again to stop.</p>

                    <div id="files">
                        <!-- iterate through files and display each title -->
                        <% if (files.length) { %>
                            <ul class="list-group">
                                <% files.forEach(function(filename){ %>
                                    <li class="list-group-item"
                                        data-filename="<%= filename %>">
                                        <%= filename.replace(/_/g, ' ').replace('.mp4', '') %>
                                    </li>
                                <% }) %>
                            </ul>
                        <% } %>
                        <!-- end file list iteration -->
                    </div>
                </div>

            </div>
        </div>
    </body>

    <script src="js/scripts.js"></script>
    <script src="js/animations.js"></script>

</html>
